// Migration script intentionally removed — per project owner's request
// Do NOT convert base64 data URLs in the codebase. Keep uploads raw and
// store only public storage URLs. This file is left as a placeholder to
// avoid accidental re-introduction of data-URL rewriting logic.
console.log('migrate_data_urls.js has been disabled by project configuration.')
